#include <iostream>
#include <string>
using namespace std;

int main() {
    string a;
    getline(cin, a);
    string b;
    getline(cin, b);
    // Print in the required format and a new line
    cout << a << b ;
    return 0;
}
